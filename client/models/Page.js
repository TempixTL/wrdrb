/**
 * All recognized pages in the Application.
 * 
 * A page-like component can navigate to any of these using the `navigate()`
 * callback.
 * @enum {string}
 */
const Page = Object.freeze({
  Home: 'home',
  OutfitLog: 'outfit log',
  Bins: 'bins',
  Wardrobe: 'wardrobe',
});

export default Page;
