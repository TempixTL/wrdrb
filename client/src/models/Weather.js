/**
 * All recognized weather types for an {@link Article}.
 * @enum {string}
 */
const Weather = Object.freeze({
  Snow: 'snow',
  Sun: 'sun',
  Rain: 'rain',
  Wind: 'wind',
});

export default Weather;