{
  "name": "wrdrb-client",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --mode=production",
    "build-dev": "webpack --mode=development",
    "watch": "npm run build-dev -- --watch",
    "clean": "rimraf ./dist/"
  },
  "author": "Tom Lauerman <tom@lauerman.dev> (https://lauerman.dev)",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "webpack": "^5.37.0",
    "webpack-cli": "^4.7.0"
  }
}
