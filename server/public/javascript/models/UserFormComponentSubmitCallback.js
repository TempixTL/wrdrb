/**
 * A callback that fires when a user form is submitted (login, register, etc.).
 * 
 * @callback UserFormComponentSubmitCallback
 * @param {Event} event
 * @param {string} username
 * @param {string} password
 */